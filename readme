Usage:
	(modificare eventualmente la variabile NORMALIZE_PROJECT_DIR con la dir corrente del progetto)
	normalize_window_ETT <file_da_normalizzare.mp3>

audacity:
	- silence finder: -26dB [lì scritto come "26"] 0,1 0,00
	- export multiples "mp3"
	- mv S.mp3 S-1.mp3
	- generate new name with  "ls > /dev/shm/lst" & "rename.c"
	- cat *.mp3 > all.mp3 + audacity + reencode

Fix:
	- forse cancella tutti gli 0,2 secondi di pausa che trova
	- redirezionare output di ffmpeg a null
